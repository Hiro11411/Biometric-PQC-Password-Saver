import cv2
import numpy as np
from PIL import Image #using LBPH algorithm
import os

path = "datasets"

def getImageID(path):
    faces=[]
    ids=
    #lambda function
    imagepath = [os.path.join(path, f) for f in os.listdir(path)] #actualize all the image from the dataset

print(getImageID(path))