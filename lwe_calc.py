#import numpy

"""
Numpy calculation for LWE, basis for understanding PQC, and the mathematics behind it. 
"""

#implement LWE and make it into an algorithm to produce, encrypted keys in a 2-3 dimensional plane

#num: int = np.random.randint(10, 20), careful and never make this into your random generator, because this is not truly random